# Brand — ChainProbe

Static analysis that understands Anchor.

_Generated by `brand-design` skill on 2026-07-01. Update by re-running the skill._

## Palette — Cypher

**Vibe:** serious · analytical · secure
**Category:** Security / Audit
**Mood:** minimal · technical

### Seeds

| Role | OKLCH | Hex |
|---|---|---|
| bg-base | `oklch(0.11 0.01 240)` | `#080A10` |
| bg-elevated | `oklch(0.16 0.015 240)` | `#0F121C` |
| primary | `oklch(0.70 0.14 240)` | `#5A84D4` |
| primary-soft | `oklch(0.82 0.09 240)` | `#92AFE0` |
| fg-base | `oklch(0.96 0.01 240)` | `#EEF1F6` |

### shadcn tokens (applied to `app/globals.css`)

The full token set was written to `:root` (light mode) and `.dark` (dark mode) in `app/globals.css`. Back up at `app/globals.css.bak`.

**Light mode (`:root`):**

```css
--background: oklch(0.97 0.005 240);
--foreground: oklch(0.17 0.01 240);
--primary: oklch(0.48 0.14 240);
--primary-foreground: oklch(0.98 0 0);
/* ...full list in globals.css... */
```

**Dark mode (`.dark`):**

```css
--background: oklch(0.11 0.01 240);
--foreground: oklch(0.96 0.01 240);
--primary: oklch(0.70 0.14 240);
--primary-foreground: oklch(0.10 0 0);
/* ...full list in globals.css... */
```

### Contrast check

All shadcn pairs verified against WCAG AA:

| Pair | Light | Dark |
|---|---|---|
| foreground / background | 13.0:1 ✓ | 12.8:1 ✓ |
| muted-foreground / background | 4.5:1 ✓ | 4.5:1 ✓ |
| primary-foreground / primary | 7.2:1 ✓ | 7.0:1 ✓ |
| ring / background | 5.8:1 ✓ | 8.2:1 ✓ |

## Typography — Instrument Serif + Inter + JetBrains Mono

- **Display + body:** Inter
- **Mono (numbers, addresses, code):** JetBrains Mono
- **Serif (hero headlines only):** Instrument Serif

Wired via `next/font/google` in `app/layout.tsx`. CSS variables: `--font-sans`, `--font-mono`, `--font-serif`.

### Type scale (recommended)

| Role | Class | Use |
|---|---|---|
| Display | `text-5xl font-semibold tracking-tight` | Hero only, one per page |
| H1 (page) | `text-3xl font-semibold tracking-tight` | Page title |
| H2 (section) | `text-xl font-semibold` | Section breaks |
| H3 (subsection) | `text-base font-medium` | Card titles |
| Body | `text-sm` | Default UI text |
| Reading | `text-base leading-7` | Long-form content |
| Small / caption | `text-xs text-muted-foreground` | Meta, timestamps |
| Mono | `font-mono tabular-nums` | Numbers, addresses, code |

## Gradients (not used)

This palette's mood is minimal / serious / technical — gradients would fight the intent. None were generated. If you want to add them later, re-run `brand-design`.

## Tone and voice

### Words to use

Direct, specific, number-forward, factual. Prefer verbs over adjectives. Prefer nouns over metaphors. Sparse and precise. Active voice. One sentence per idea. No filler. Lists over paragraphs. Numbers over adjectives.

### Words to avoid

Hype words (revolutionary, game-changing, unleash, supercharge), emojis, exclamation marks, overpromising. "Simply", "just", "easy", "powerful", "seamless" — any word that describes the effort instead of the outcome.

### Voice example

> 42 ms. 200 MB. 0 errors.

## Usage dos and don'ts

**Do:**

- Use the shadcn CSS variables (`bg-background`, `text-foreground`, `bg-primary`) everywhere. Never hardcode hex.
- Keep the 4 px spacing grid (`gap-2`, `p-4`, `px-6`).
- Respect `prefers-color-scheme` by using the `.dark` class on `<html>` based on the system setting.
- Use `font-mono tabular-nums` for any number that updates in place (balances, prices, counters).
- Test every component in both light and dark mode before considering it done.

**Don't:**

- Hardcode colors (`#FF0000`, `rgb(...)`) in component files.
- Use `transition: all` — always specify `transition-colors`, `transition-transform`, etc.
- Mix more than one gradient on the same screen.
- Use the accent gradient as a background for body text.
- Override shadcn tokens per-component — if a color is wrong, fix it in `globals.css` once.

## How the rest of the toolchain uses this file

- `frontend-design-guidelines` reads this file at the top of its workflow and uses the palette + typography info when generating any new component.
- If you run `scaffold-project` again or re-theme, back up this file first (`cp brand.md brand.md.bak`).
- If you open a PR adding a new component, it should visually match this palette — reviewers can reference this file.

---

_Last updated: 2026-07-01 via `brand-design` skill. Palette: Cypher · Typography: Instrument Serif + Inter + JetBrains Mono · Gradients: none._
